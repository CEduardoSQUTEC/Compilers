entero mcd(entero a, entero b) {
	entero temp;

	mientras (b != 0) {
		temp = b;
		b = a % b;
		a = temp;
	}

	retorno a;
}

sin_tipo main() {
	entero a;
	entero b;
	entero r;

	a = entrada();
	b = entrada();

	r = mcd(a, b);

	salida(r);
}
